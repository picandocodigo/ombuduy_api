<% unless @issues.empty? %>
  <h3>Reportes sin resolver</h3>
  <ol>
  <% @issues.each do |issue| %>
    <li><%= link_to issue.text, issue_path(issue) %> (<%= issue.fixed %>)</li>
  <% end %>
  </ol>
<% end %>
